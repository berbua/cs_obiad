// Simple Clippy.js wrapper for agents
(function() {
  window.clippy = {
    agents: {},
    
    load: function(name, callback) {
      var BASE_PATH = '/agents/';
      var agent = new Agent(BASE_PATH + name + '/', name);
      
      agent.load(function() {
        window.clippy.agents[name] = agent;
        if (callback) callback(agent);
      });
    }
  };
  
  function Agent(path, name) {
    this.path = path;
    this.name = name;
    this._data = null;
    this._el = null;
    this._animator = null;
    this._balloon = null;
    this._queue = [];
    this._isLoaded = false;
  }
  
  Agent.prototype.load = function(callback) {
    var self = this;
    
    $.getScript(this.path + 'agent.js', function() {
      self._data = window[self.name + 'AgentData'];
      self._setupElement();
      self._isLoaded = true;
      
      // Load sounds
      $.getScript(self.path + 'sounds-mp3.js', function() {
        if (callback) callback();
      });
    });
  };
  
  Agent.prototype._setupElement = function() {
    this._el = $('<div class="clippy-agent"></div>');
    this._el.css({
      position: 'fixed',
      bottom: '10px',
      right: '10px',
      width: '124px',
      height: '93px',
      backgroundImage: 'url(' + this.path + 'map.png)',
      backgroundRepeat: 'no-repeat',
      zIndex: 10000,
      cursor: 'pointer'
    });
    
    this._balloon = $('<div class="clippy-balloon"></div>');
    this._balloon.css({
      position: 'fixed',
      bottom: '110px',
      right: '10px',
      backgroundColor: '#FFFFCC',
      border: '2px solid #000',
      borderRadius: '5px',
      padding: '10px',
      maxWidth: '200px',
      display: 'none',
      zIndex: 10001,
      fontFamily: 'Comic Sans MS, cursive',
      fontSize: '12px',
      boxShadow: '2px 2px 5px rgba(0,0,0,0.3)'
    });
    
    // Random messages when clicked
    var messages = [
      'Hej! Kliknąłeś mnie! 📎',
      'Witaj! Czy wiesz, że jestem klasycznym asystentem z MS Office? 🎓',
      'Pamiętasz mnie z lat 90? Te czasy były wspaniałe! 🌟',
      'Potrzebujesz pomocy z czymś? A może po prostu obiad? 🍕',
      'Jestem tu żeby pomóc! Albo przynajmniej się starać... 😅',
      'Czy zapisałeś się już na obiad? To ważne! 🍔',
      'Kliknij przycisk z muzyką - Nokia Tune to klasyka! 🎵',
      'Pamiętaj żeby dać lajka swoim kolegom! 👍',
      'Netscape Navigator 4.0 - to były czasy! 🌐',
      'Lubię tę stronę! Ma klimat vintage! ✨',
      'Czy próbowałeś już wszystkich funkcji? 🤔',
      'Jestem twój przyjaciel i asystentem! 🤝',
      'Comic Sans to najlepszy font! Fight me! 😤',
      'WordArt + GIF-y = perfection! 💯',
      'Czasami tęsknię za Windows 98... 💙',
      'Między nami - pilnuję też bezpieczeństwa! 🔒',
      'Próbowałeś wpisać <script>? Nice try! 😏',
      'Jestem jak firewall, tylko przyjemniejszy! 🛡️',
      'Hakerzy mnie nie lubią, ale ja ich pilnuję! 👀',
      'Bezpieczeństwo to moja druga pasja po obiadach! 🚨'
    ];
    
    var self = this;
    this._el.click(function() {
      var randomMessage = messages[Math.floor(Math.random() * messages.length)];
      self.speak(randomMessage);
      self.play('GetAttention');
    });
    
    $('body').append(this._el);
    $('body').append(this._balloon);
    
    this._currentFrame = 0;
    this._currentAnimation = null;
  };
  
  Agent.prototype.show = function() {
    if (this._el) {
      this._el.show();
    }
  };
  
  Agent.prototype.hide = function() {
    if (this._el) {
      this._el.hide();
    }
    if (this._balloon) {
      this._balloon.hide();
    }
  };
  
  Agent.prototype.speak = function(text) {
    var self = this;
    if (this._balloon) {
      this._balloon.text(text);
      this._balloon.fadeIn(300);
      
      setTimeout(function() {
        self._balloon.fadeOut(300);
      }, 3000 + (text.length * 30));
    }
  };
  
  Agent.prototype.play = function(animationName) {
    if (!this._el) return;
    
    // Map animation names to different sprite positions or CSS animations
    var animations = {
      'Wave': { frames: [0, 1, 2, 1, 0], duration: 500 },
      'GetAttention': { frames: [0, 3, 0, 3], duration: 400 },
      'Congratulate': { frames: [0, 1, 2, 3, 2, 1], duration: 600 },
      'GetTechy': { frames: [0, 2, 4, 2], duration: 500 },
      'Sad': { frames: [0, 5], duration: 800 },
      'Thinking': { frames: [0, 1, 0], duration: 700 },
      'LookDown': { frames: [0, 6], duration: 500 },
      'LookUp': { frames: [0, 7], duration: 500 },
      'LookLeft': { frames: [0, 8], duration: 500 },
      'LookRight': { frames: [0, 9], duration: 500 },
      'Reading': { frames: [0, 1, 2], duration: 600 },
      'Writing': { frames: [0, 2, 4], duration: 600 },
      'GetWizardy': { frames: [0, 3, 6, 3], duration: 500 },
      'CheckingSomething': { frames: [0, 1, 3, 1], duration: 500 },
      'Searching': { frames: [0, 2, 4, 6], duration: 600 },
      'RestPose': { frames: [0], duration: 500 },
      'Alert': { frames: [0, 3, 0], duration: 300 }
    };
    
    var animation = animations[animationName] || animations['Wave'];
    var self = this;
    var frameIndex = 0;
    
    // Add bounce/wiggle animation
    this._el.addClass('clippy-animate-' + animationName.toLowerCase());
    
    var animateFrames = function() {
      if (frameIndex < animation.frames.length) {
        var frame = animation.frames[frameIndex];
        var x = (frame % 10) * -124;
        var y = Math.floor(frame / 10) * -93;
        
        self._el.css({
          backgroundPosition: x + 'px ' + y + 'px'
        });
        
        frameIndex++;
        setTimeout(animateFrames, animation.duration / animation.frames.length);
      } else {
        // Reset to default pose
        self._el.css({
          backgroundPosition: '0px 0px'
        });
        setTimeout(function() {
          self._el.removeClass('clippy-animate-' + animationName.toLowerCase());
        }, 100);
      }
    };
    
    animateFrames();
  };
  
  Agent.prototype.animate = function() {
    return this.play.apply(this, arguments);
  };
})();
